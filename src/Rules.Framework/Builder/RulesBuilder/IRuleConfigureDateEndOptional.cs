namespace Rules.Framework.Builder.RulesBuilder
{
    /// <summary>
    /// Configurer for a rule's optional end date or proceed to remaining rule build options.
    /// </summary>
    public interface IRuleConfigureDateEndOptional : IRuleConfigureDateEnd, IRuleBuilder
    {
    }
}